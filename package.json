{
  "name": "finchain-dapp",
  "version": "1.0.0",
  "description": "Decentralized Financial Transaction System using Blockchain and MetaMask",
  "scripts": {
    "install:all": "npm install --prefix blockchain && npm install --prefix client",
    "dev:client": "npm run dev --prefix client",
    "compile:contract": "npm run compile --prefix blockchain",
    "deploy:local": "npm run deploy:local --prefix blockchain",
    "deploy:sepolia": "npm run deploy:sepolia --prefix blockchain",
    "node:local": "npm run node --prefix blockchain"
  },
  "author": "",
  "license": "MIT"
}
